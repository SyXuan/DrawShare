<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= image.name %>的繪圖作品 - DrawShare</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-color: #f8f9fa;
        }
        .image-container {
            background-color: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .drawing-image {
            width: 100%;
            max-height: 70vh;
            object-fit: contain;
        }
        .action-buttons {
            display: flex;
            gap: 10px;
            margin-top: 20px;
        }
        .btn {
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            cursor: pointer;
            transition: all 0.2s;
        }
        .btn-primary {
            background-color: #4f46e5;
            color: white;
        }
        .btn-primary:hover {
            background-color: #4338ca;
        }
        .btn-secondary {
            background-color: #e5e7eb;
            color: #4b5563;
        }
        .btn-secondary:hover {
            background-color: #d1d5db;
        }
    </style>
</head>
<body class="p-6">
    <div class="container mx-auto max-w-4xl">
        <div class="flex justify-between items-center mb-6">
            <a href="/images" class="text-indigo-600 hover:text-indigo-800 flex items-center">
                <i class="fas fa-arrow-left mr-2"></i>返回所有作品
            </a>
            <h1 class="text-2xl font-bold text-gray-800"><%= image.name %>的繪圖作品</h1>
        </div>
        
        <div class="image-container mb-6">
            <img src="/public/images/<%= image.image_file_name %>" alt="<%= image.name %>的繪圖" class="drawing-image">
            <div class="p-4 border-t border-gray-200">
                <div class="flex justify-between items-center">
                    <div>
                        <h3 class="font-medium text-gray-800"><%= image.name %>的繪圖作品</h3>
                        <p class="text-sm text-gray-500">上傳於 <%= new Date(image.uploaded_at).toLocaleString('zh-TW') %></p>
                    </div>
                    <a href="/public/images/<%= image.image_file_name %>" download class="text-gray-500 hover:text-gray-700">
                        <i class="fas fa-download text-lg"></i>
                    </a>
                </div>
            </div>
        </div>
        
        <div class="action-buttons">
            <a href="/images" class="btn btn-secondary flex-1">
                <i class="fas fa-images"></i> 查看所有作品
            </a>
            <a href="/" class="btn btn-primary flex-1">
                <i class="fas fa-paint-brush"></i> 創建新繪圖
            </a>
        </div>
    </div>
</body>
</html>